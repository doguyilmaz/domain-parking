[build]
  publish = "."

# Catch-all so the parking page renders for any path the visitor types.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# Headers live in the _headers file (shared with Cloudflare).
